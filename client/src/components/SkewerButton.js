import React, { useState } from 'react';


const SkewerButton = ({ onClick }) => {

    return (
        <div>
            Hello World

            {props.children}
        </div>


    )
};

export default SkewerButton;